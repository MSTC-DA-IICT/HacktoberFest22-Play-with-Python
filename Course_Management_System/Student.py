from tkinter import *
from PIL import *

root = Tk()

root.title("Course Management System")

lbl_title = Label(root, text='Course Management System')

Main_frame = Frame(root)

up_frame = LabelFrame(Main_frame)

upper_frame1 = Frame(up_frame)

# get the image on the button
img = Button(up_frame, <image_file>)

# Label for DBMS
lbl_2 = Label(upper_frame2,text="DBMS")
# when the student click on the label DBMS then it redirect to the Course page


# Create other courses and there course pages

root.mainloop()